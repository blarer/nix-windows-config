local branch="$1"
if [[ -z "$branch" ]]; then
  echo "Usage: gwt <branch-name>"
  return 1
fi
local target="../${PWD:t}-${branch}"
git worktree add "$target" -b "$branch" 2>/dev/null || \
  git worktree add "$target" "$branch"
cd "$target"
